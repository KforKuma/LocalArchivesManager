# Expected results

Machine-readable expected behavior is stored per case in `../manifest.json`.
This directory is reserved for future stable golden outputs that are too large
or structured to keep inline in the manifest. Golden files must not contain
absolute paths, timestamps, provider responses, or user-library data.
